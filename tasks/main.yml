---

- include: 00-git.yml
- include: 10-yarn.yml
- name: Install packages and run build
  shell: |
    cd {{ project_root }}
    yarn install
    npm run build
  become: true

- include: 20-pm2.yml
